# Registry Test Package

This package contains unit tests for specialized registry classes.

Each test verifies:
- Expected entities are registered
- Entity count is within expected range
- Registry follows correct naming pattern