# Real World STEP Corpus

This directory contains real-world STEP files for testing.

## Structure

- `corpus-manifest.tsv` - Manifest of test files
- `local-only/` - Local-only test files (not checked into git)