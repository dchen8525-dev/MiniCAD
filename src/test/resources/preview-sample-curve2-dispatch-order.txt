# Branch order of PcurveSamplingHelper.sampleCurve2, captured from the original
# 5-branch `if (curve instanceof X)` chain before it was folded into
# PREVIEW_SAMPLE_CURVE2_RULES. Every branch returns, so this is first-match-wins
# dispatch: instanceof also matches subtypes, so a dropped, duplicated or
# reordered rule silently changes which curve is sampled how. Curves matching no
# rule fall through to the terminal `return List.of();`. All 5 types are final
# classes implementing Curve2 directly (no subtype relation today), so the order
# is currently behaviour neutral -- the frozen file turns any future reordering
# into a test failure rather than a silent behaviour change.
Line2
Circle2
Ellipse2
BSplineCurve2
TrimmedCurve2
