# Branch order of StepPmiPayloadBuilder.pointFromPlaceholderItem, captured from
# the original `if (item instanceof X)` chain before it was folded into
# PLACEHOLDER_POINT_RULES.
#
# Every branch returns, so this is first-match-wins dispatch. All four types are
# final classes implementing StepEntity directly (no subtype relation today), so
# the order is currently behaviour neutral -- the frozen file turns any future
# reordering, drop or duplication into a test failure rather than a silent change
# in which placeholder point is picked.
StepGeometricSet
StepGeometricCurveSet
StepPointSet
StepAnnotationPlane
