# Branch order of PreviewFaceBuilder.toRectangularCompositeSurfaceFacePayload,
# captured from the original 5-branch `if (basis instanceof X)` chain before it
# was folded into COMPOSITE_BASIS_FACE_RULES. Every branch returns, so this is
# first-match-wins dispatch: instanceof also matches subtypes, so a dropped,
# duplicated or reordered rule silently changes which payload builder is used.
# A basis matching no rule returns null, as the old trailing statement did. All
# 5 types are final classes implementing StepEntity directly (no subtype
# relation today), so the order is currently behaviour neutral -- the frozen
# file turns any future reordering into a test failure rather than a silent
# behaviour change.
StepCylindricalSurface
StepConicalSurface
StepSphericalSurface
StepToroidalSurface
StepPlane
