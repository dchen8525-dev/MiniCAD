# Branch order of StepCadBuilder.buildSurfaceGeometry, captured from the
# original `if (entity instanceof X)` chain before it was folded into
# PARAMETRIC_SURFACE_RULES.
#
# Every branch returns, so this is first-match-wins dispatch. All four types are
# final classes implementing StepEntity directly (no subtype relation today), so
# the order is currently behaviour neutral -- the frozen file turns any future
# reordering, drop or duplication into a test failure rather than a silent change
# in which typed builder a parametric surface id is routed to.
#
# Note: the same four (type -> builder) pairs also appear in the instance-built
# supportedFaceGeometryRules / supportedSurfaceGeometryRules tables (idFaceRule /
# idRule). Those dispatch by entity and drive different entry points; this table
# is the id-based parametric-surface entry and is frozen separately on purpose.
StepParaboloidSurface
StepHyperboloidSurface
StepSurfaceOfTranslation
StepSurfaceOfProjection
