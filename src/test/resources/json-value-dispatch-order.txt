# Branch order of PreviewSerializers.appendJsonValue, captured from the
# original `if (value instanceof X)` chain before it was folded into
# JSON_VALUE_RULES.
#
# Every branch returns, so this is first-match-wins dispatch. The original chain
# used two OR-ed branches -- (Boolean || Integer || Long) and (Float || Double)
# -- which are split here into one rule per type; the types are mutually
# exclusive and share a writer, so the split is behaviour neutral. The frozen
# order turns any future reordering, drop or duplication into a test failure
# rather than a silent change in how a value is serialised.
String
Boolean
Integer
Long
Float
Double
Map
List
