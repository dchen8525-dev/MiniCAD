# Branch order of StepCadTopologyBuilder.projectOntoCurve, captured from the
# original 9-branch `if (curve instanceof X)` chain before it was folded into
# PROJECT_ONTO_CURVE_RULES.
#
# Every branch returns, so this is first-match-wins dispatch: instanceof also
# matches subtypes, so a dropped, duplicated or reordered rule silently changes
# which curve is projected how. Curves matching no rule fall through to the
# trailing `return point` fallback, so the frozen file also protects that
# boundary. All 9 types are final classes implementing Curve3 directly (no
# subtype relation today), so the order is currently behaviour neutral -- the
# frozen file turns any future reordering into a test failure rather than a
# silent behaviour change.
BSplineCurve3
RationalBSplineCurve3
Line3
Circle
Ellipse3
Polyline3
TrimmedCurve3
SurfaceCurve3
CompositeCurve3
